name: delta_os_core
description: DeltaOS - Coordination Layer Operating System. Makes solutions work together instead of fighting each other.
version: 0.1.0-alpha
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0

# Add executable for CLI
executables:
  delta_os_core:
